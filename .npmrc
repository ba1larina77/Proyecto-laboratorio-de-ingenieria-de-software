# Iteración 3 — Bypass del conflicto peer entre @testing-library/react@16
# y los @types/react@19 declarados como devDependency.
# El runtime real es React 18.3.1 (ver peerDependencies en package.json),
# por lo que la instalación funciona correctamente con esta bandera.
legacy-peer-deps=true
